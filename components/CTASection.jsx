import alexerPro from '@/lib/fonts';

export const CTASection = () => {

    return (
        <div className="grid grid-cols-12 gap-[24px] px-[64px] h-[532px] bg-black">
            <h2 className={`${alexerPro.className} text-8xl col-span-7 col-start-1 text-white mt-[76px]`}>UNETE A ERÖS CLUB</h2>
        </div>
    )

}